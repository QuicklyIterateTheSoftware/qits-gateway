# Build outputs
target/
target-ide/

# Quinoa's package-manager-install directory (the node/pnpm it provisions when a build asks it to).
# The image build is the only thing here that asks; this keeps a stray local one out of the tree.
.quinoa/

# IDE
.vscode/
.idea/
.project
.classpath
.factorypath
.settings/
*.iml

# OS
.DS_Store

# Home-relative caches a tool leaks into the repo root when run with the repo as its cwd
.cache/
.m2/
.local/
