# How qits-platform-deployments deploys this repo. It reads this file from the git host at
# the built sha, so the answer travels with the commit.
#
# CARRYING THE FILE AT ALL is the other thing it says. qits-workspaces reads it on a release to
# decide whether this repository deploys — a library or an SPA carries none and its release is
# the trunk push alone — and then promotes onto the branch it is configured with
# (`qits.workspaces.release.entry-branch`). `deploy_branches` used to name that branch here, in
# thirteen copies of one platform-wide answer, and a release pushed onto every entry it named.
#
# ENVIRONMENT service: every environment runs its own gateway. The platform edge fronts them
# all and owns the host port, so a gateway no longer binds one itself. A green build deploys
# into whichever tier listens to the built branch.
deployment_target: environment

# PUBLIC NODE: every environment's gateway is that environment's public node. It joins the
# environment's bundle network and every per-application network in it, and those joins are how
# it reaches its own environment's applications once the legacy network is empty.
available_on_env: true

# The gateway owns the root path space, so the convention /gateway/q/health/ready would 404.
health_path: /q/health/ready
